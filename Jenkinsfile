pipeline {
    agent { 
        any { 
            image 'node:12.16.2'
        } 
    }
    stages {
        stage('Build') {
            steps {
                sh 'node --version'
            }
        }
    }
}